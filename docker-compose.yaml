version: "5.0"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - .:/frontend
      - /frontend/node_modules
    environment:
      NODE_ENV: development